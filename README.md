# Life Remake

> 真正可控的叙事 Agent，让每一段 TRPG 人生不再漂移。

**Life Remake** 是一个以人生经历为载体的可控叙事游戏。玩家定义人物、选择天赋并在关键时刻作出抉择；模型负责叙事视角与文字表现，规则引擎负责主线节拍、事实、数值、存档和结局审批。

[官方体验站（持续更新）](http://47.98.121.127/) · [快速开始](#快速开始) · [架构说明](./docs/ARCHITECTURE.md) · [使用流程](./docs/USAGE_FLOW.md)

![人生时间线：同年经历、属性档位、命运人物与抉择后果](./docs/assets/readme/screenshots/3badb820dfa99846396fb1babed6769d.png)

*一次选择，留在此后的经历里。图为古代权谋的一局实机画面。*

## 为什么是“可控叙事 Agent”

纯文本角色扮演常见的问题，是模型要么只会顺着最近一句话续写，要么为了保持连续性把玩家带入无法验证的路线。Life Remake 将创作与裁决拆开：

- **模型选择叙事视角，不直接改写游戏规则。** 每回合模型从当前世界包的完整路线与阵营目录中选择视角，并提交背景、场景或抉择；引擎只接受世界包中存在的 ID。
- **工具调用承载协议，正文只承载体验。** 模型通过 `render_background_segment`、`render_scene` 或 `render_choice_scene` 提交结构化回合；玩家界面只显示自然叙事、抉择与已结算结果。
- **世界主线提供阶段方向。** 古代权谋、奇幻修仙和现代都市均已配置三幕主线；每幕允许路线自由切换，通过 `setup -> escalation -> pressure -> climax -> payoff` 形成阶段结果。
- **状态由引擎记录与结算。** 世界幕节拍、事实、属性、天赋、人物、地点、本领、生存危机和结局蓝图由后端维护；前端展示人物档案、属性档位与经历结果，内部协议不作为正文展示。

这不是把大段事件列表一次塞给模型，也不是用固定脚本替代生成。模型在世界、角色和近期叙事的压缩上下文中自由选择当前经历视角；引擎记录世界幕节拍、已发生事实和受控后果，使所有场景服务于同一条世界主线。

## 游戏体验

玩家看到的是一条可向上回看的自然人生时间线：平缓年份沉淀成长、关系和余波，矛盾逐渐累积为连续场景，并在关键处给出具体的选择。

- 开局设定人设、五维属性和 1 至 3 张天赋卡，自动生成可折叠阅读的身世与天赋画像。
- 每幕开场前选择成长方向；普通年份承载生活、年龄增长与属性积累，满足属性准入后再进入相应主线场景。
- 重大矛盾可以停留在同龄连续抉择中，避免为了年份刻度打断冲突。
- 同一世界中可以在不同经历之间自然切换；开放问题按需召回，已结束事件保留为历史结果，人物关系、足迹和本领继续积累。
- 体魄长期低于当前年龄阶段的风险线时，才可能进入生存危机；玩家可以选择自愈、请求帮助或听天由命。失败才会死亡并进入带死因的结算。
- 主线完成后，模型只能申请收束；引擎结合属性、天赋、历史代价与名望锁定好、普通或坏结局的方向。

### 三个可选世界

世界选择由后端内容列表提供，本地与云端均使用这一入口。仓库已提供三套 v6 叙事世界包，分别配置文风、三幕、阵营、经历路线、成长方向、生存规则及三档结局。

| 世界 | 阶段中心 | 人生体验 |
| --- | --- | --- |
| 古代权谋 | 案件入局 → 阵营立身 → 战乱担当 | 案件与现实纷争、组织中的历练与周旋、战乱中的行动与承担。 |
| 奇幻世界（修仙方向） | 成长入道 → 宗门历练 → 破境问道 | 诡异、异族与宗门争衡中的奇遇、本领积累、历练与道途跨越。 |
| 现代都市 | 学习成长 → 选择与迷茫 → 关系与成就 | 学习、升学求职、独立生活及长期关系；婚姻和事业并非唯一归宿。 |

这些中心提供题材方向，具体案件、组织、人物、经历与结果由本局叙事生成。

### 古代权谋的三幕示例

古代世界不是六条互斥的职业线。家门、仕途、商路、法度、边地与人情等经历始终开放，模型根据当下人物、已发生事实和当前世界幕选择视角；它们共同把人物带过三层递进的矛盾：

1. **案起入局**：从自身身份和生活中接触一桩案件、制度裂缝或未被承担的现实矛盾；案由可以是新事，也可以带着旧线索。
2. **立身周旋**：承接前一幕的后果，进入阵营、组织、地方势力或关系网络，在新的位置面对独立矛盾。
3. **大局担当**：外部战争进入人物与社会的生活；根据已有身份和能力参与组织调度、交锋进退、保护或求存，呈现战事的实际结果和战后处境。

每幕由 `setup -> escalation -> pressure -> climax -> payoff` 的全局节拍承载。模型每拍都可更换经历视角；`payoff` 完成当前世界幕，新幕从开场节拍开始，人物、事实、属性和历史抉择继续带入。前幕形成的处境连接新幕，已解决的事情作为结果留存。

## Agent 运行链路

![Life Remake 叙事 Agent：上下文、工具调用、引擎结算与存档](./docs/assets/readme/narrative-agent-flow.svg)

```text
压缩消息 + 世界包 + 已发生事实 + 路线/阵营目录
                    |
                    v
模型工具调用（背景 / 场景 / 抉择）
                    |
                    v
引擎审核世界 ID、属性语义、人物引用、事实与年龄
                    |
                    v
引擎校验并结算 -> TurnRecord -> 流式时间线 / 存档
```

当三幕世界主线完成，模型工具集切换为 `request_story_closure`。它只能申请结局，不能自行宣布结束；引擎审核前置事实与收束状态，锁定结局蓝图后再生成结算文本。

请求前按任务组织人物、天赋、近期叙事、世界知识、事实与资产；模型在生成时同步提交变化，回合提交后分别更新账本、人物、地点、本领与历史。摘要异步更新，已收束事实退出待解决问题，相关结果仍可被后续需要的场景召回。

普通年份工具描述生活与成长；场景工具承载当前幕与节拍任务。两者共享一次动态请求，由现有准入与模型工具选择衔接。这套分层借鉴沉浸式文本应用的上下文组织方式，运行时由本项目自身维护，不依赖安装 SillyTavern，也未接入向量 RAG。

## 界面预览

墨色底、暖金点缀，正文居中，属性与人物档案分列两侧。成长方向与结算沿用同一套卡片和弹窗风格。

<details>
<summary>选择此阶段的积累 · 成长方向卡片</summary>

为下一段人生选择更看重的积累，日常成长随之侧重。

![成长方向卡片：见识与谋断、人情与根基、历练与韧性](./docs/assets/readme/screenshots/18af1cf9a06bf104ba4d59c6f77f3e28.png)

</details>

<details>
<summary>回望这一生 · 结算画面（含剧情示例）</summary>

结算以名望、称号与本局经历的收束文字呈现人生归宿。

![本局结算：名望、称号与人生收束文本](./docs/assets/readme/screenshots/96c1869420d5c2f8c77f2722c730ea6a.png)

</details>

截图记录具体版本与对局，文字、数值和可见模块会随更新变化。[截图目录与维护说明](./docs/assets/readme/screenshots/README.md)。

## 核心能力

| 能力 | 说明 |
| --- | --- |
| 可控叙事 | 单次 `tool_call`：模型提交背景、场景或抉择；引擎审批受控状态，正文只服务于玩家体验。 |
| 世界主线 | 世界包定义主线幕、路线目录、阵营、Lore、事实与结局蓝图；引擎不绑定古代世界。 |
| 幕间节拍 | 每个世界幕保存 `setup -> escalation -> pressure -> climax -> payoff`；模型每拍都可自由切换路线视角。 |
| 原子回合 | `TurnRecord` 固化每个已结算回合的文本、抉择、公开属性快照和年龄范围，前端只读取该公开投影。 |
| 生存危机 | 年度体魄风险经宽限期与年龄阶段规则审核后才出现；三种求生选择以智力、魅力或气运档位结算。 |
| 天赋与人物 | 玩家仅携带已选的 1 至 3 张天赋卡进入模型上下文；可回归人物使用稳定身份引用，前端以“命运人物”投影展示。 |
| 足迹与本领 | 地点和技能随叙事生成、更新并在局内保存，侧栏与回合变化同步展示，后续按任务召回。 |
| 模型用量 | Setting 内展示本会话模型请求与服务商已上报的 Token 用量，不估算账户余额或精确费用。 |
| 匿名存档 | 无需账号即可保存、恢复、删除分支存档，并可通过恢复码在新的匿名会话中恢复。 |
| 流式体验 | 后端以 NDJSON 推送已完成的回合，前端以独立可滚动的阅读时间线呈现。 |

### 生存与死亡

生存风险按实际跨过的年份累计，同年连续场景不会重复累计年度风险。当前三个世界都配置了幼年、青年、成年与晚年的体魄风险线：连续低于风险线满 3 个检查年份后开始概率判断，风险随持续年份增长并有阶段上限；体魄恢复或风险阶段切换时重新累计。

触发的是濒死抉择。自愈、请求帮助、听天由命分别使用智力、魅力、气运档位判断，最高档必定成功；成功后体魄至少恢复至当前风险线加 5，并清除低体魄累计。失败才死亡，由引擎确定死因，再由模型结合此前经历渲染结算。

家境也会按档位在每个实际跨年提供额外体魄变化，具体权重由世界包配置。年龄影响生活与生存风险，不作为主线强制结束的上限。

## 快速开始

### 环境

- Windows 10/11
- Node.js `24.x`（项目当前开发与验证环境：Node `24.14.0`、npm `11.9.0`）
- 一个支持 OpenAI 兼容 Chat Completions 的模型服务 API Key

### 一键启动

1. 克隆或下载本仓库并进入项目根目录。
2. 双击 `start-local.bat`，或在 PowerShell 中执行：

   ```powershell
   .\start-local.bat
   ```

3. 首次运行时，脚本会在依赖缺失时安装依赖，并由 `apps/backend/.env.example` 自动创建 `apps/backend/.env`。
4. 浏览器打开 `http://localhost:5173`，在 Setting 中填写 API Key、服务地址和模型名，确认本局环境后开始游戏。

脚本会释放本项目使用的 `5173` 和 `4000` 端口后启动前后端开发链路。已有其他服务占用这两个端口时，请先确认可以关闭它们。

### 模型配置

当前对外支持 **OpenAI 兼容的 `/chat/completions`** 接口。默认模板已配置为：

```dotenv
DEFAULT_PROVIDER_BASE_URL=https://api.deepseek.com
DEFAULT_PROVIDER_MODEL=deepseek-v4-flash
DEFAULT_PROVIDER_API_PATH=/chat/completions
```

本地模式下，API Key 随当前游戏会话提交给后端进程使用，不写入项目的持久化存档；浏览器只记住服务地址、模型等非密钥配置。请勿将含有真实密钥的 `.env` 或运行时配置提交到版本库。

<details>
<summary>查看本地 Setting 配置界面</summary>

在同一界面填写模型服务参数并确认世界；图中密钥以密码框遮蔽。

![本地 Setting：模型服务地址、模型名、接口路径与世界选择](./docs/assets/readme/screenshots/79fb39d844ba4b0bbc637b4e24b353b8.png)

</details>

云端模式通过 `start-cloud.bat` 使用服务器配置的模型服务，玩家无需填写密钥；本地与云端仍是独立配置链路。世界可在 Setting 选择，难度选择目前不在前端开放，沿用内容列表的默认首项。

## 存档与数据

- 浏览器通过 HttpOnly 匿名会话 Cookie 识别当前玩家，不需要注册或登录。
- 当前局、存档槽和存档快照存储于 `storage/anonymous-game-store.json`；运行中的局默认保留 7 天，匿名会话默认 30 天，存档默认 180 天。具体期限和槽位数可通过 `ANONYMOUS_*` 环境变量调整。
- 每个匿名会话默认最多 5 个存档。恢复一个存档会创建新的当前局，原存档保留为分支点；恢复码可把存档带到新的匿名会话。
- 抉择前的自动检查点用于回到此前分支，不占手动存档槽位。
- “重置匿名存档”会清除当前匿名会话下的运行局和全部存档，使浏览器回到新用户状态。

公开部署时，请将 `storage/` 放在持久化磁盘或卷上。项目当前的文件仓储适合单后端实例；扩展为多实例前，应在保留存储接口的前提下替换为共享仓储。

生成所需的人设、天赋、近期故事和相关记忆会发送给配置的模型服务商。匿名存档免去账号注册，并不意味着这些叙事请求只在浏览器内处理。

## 扩展一个新世界

引擎不依赖古代世界的路线数、名称或年龄边界。新世界可通过世界包扩展，而不需要重写核心叙事状态机：

1. 从 [`examples/world-pack`](./examples/world-pack/WORLD_PACK_GUIDE.txt) 复制 `world.example.json` 与 `story.example.json`，同步替换世界 ID。
2. 分别放入 `data/worlds/<worldId>.json` 和 `data/narratives/<worldId>.story.json`；基础文件提供前端名称和年龄阶段，叙事文件提供文风、幕、阵营、路线、Lore、成长与结局配置。
3. 每条路线配置好／普通／坏三个结局蓝图。当前动态场景由模型生成，不需要为每条路线每个节拍补齐静态事件；人物、地点、本领和新增事实在对局中记录。

首次初始化会把基础世界列表写入 `storage/custom-content.json`。已有内容存储的安装需要同步其 `worlds` 列表，单独复制基础文件不会自动追加选项；叙事包有进程缓存，修改后需重启后端。模板与三个内置世界包可共同作为扩展参考，生存配置见各包的 `progression.survival`。

详细的内容制作说明见 [小说蒸馏与世界包指南](./docs/NOVEL_DISTILLATION_GUIDE.md) 和 [配置指南](./docs/CONFIG_GUIDE.md)。

## 工程与技术栈

```text
apps/
  backend/   Express + TypeScript：流式 API、规则引擎、工具审批、存档仓储、模型适配
  frontend/  React 18 + Vite：阅读器时间线、抉择、存档与本地模型设置
packages/
  shared/    TypeScript 共享契约：运行态、工具载荷、TurnRecord、世界包结构
data/        世界线、叙事世界包、事件、阵营、天赋等数据素材
storage/     单实例运行期存档与配置（不应提交）
```

- **语言与运行时**：TypeScript、Node.js
- **AI 应用能力**：OpenAI Compatible API、受控 function calling、结构化输出、分层消息上下文、工具结果审批
- **游戏引擎能力**：数据驱动世界包、事实账本、世界幕五拍、属性与代价结算、生存危机、三档结局蓝图
- **交付体验**：React、Vite、Express、NDJSON 流、匿名会话与存档恢复

## 后续方向

- **数据库存储**：在现有匿名会话、回合与存档接口基础上演进持久化，改善数据管理和部署扩展能力。当前仍使用文件仓储，尚未指定数据库选型。
- **前端 UI／UX 交互**：围绕时间线推进、抉择出场、属性反馈、人物与足迹本领、结局呈现优化组件和过渡体验。

以上为后续 issue 方向，具体范围与排期会随迭代确定。

## 文档

- [架构与运行链路](./docs/ARCHITECTURE.md)
- [技术细节](./docs/TECHNICAL.md)
- [使用流程](./docs/USAGE_FLOW.md)
- [模型与运行配置](./docs/CONFIG_GUIDE.md)
- [世界包和素材制作](./docs/NOVEL_DISTILLATION_GUIDE.md)
- [更新日志](./docs/CHANGELOG.md)
- [开发日志](./docs/DEV_LOG.md)

## 许可与第三方服务

本项目代码采用 [MIT License](./LICENSE)。通过本项目调用的模型服务由使用者自行选择和配置，费用、数据处理与输出使用应遵守对应服务商的条款。项目依赖保留各自许可证。

Life Remake 正在持续迭代中，欢迎保持期待。
