# 更新日志

## 2026-09-20 03:44 +08:00 — v9 开放世界包与语义节拍观察

- 三个内置世界包从固定路线目录迁移为 `worldCore + socialForces + narrativePalette + storyPatterns + worldCards`；古代、奇幻、现代分别重写世界底座与题材可能性，并移除与故事形态重复的路线视角卡。
- 身世后新增本局故事前提生成，玩家人设、最终天赋和身世共同决定人物锚点、长期张力、故事承诺与三个结构段的问题。
- Turn Planner 改为可选故事形态和社会力量；精确召回、动态场景、抉择记录与 Episode 关联同步使用新协议，不再要求 route ID。
- 新增独立 `observe_story_beat` 请求。模型只判断当前正文是否完成当前拍的语义职责，引擎保留唯一推进权；抉择在玩家作答后再判定。
- v9 结局改为世界级好／普通／坏三张蓝图，不再绑定最终路线。社区示例、README 与世界包说明同步更新。

## 2026-09-16 19:05 +08:00 — 世界卡一致性补全

- 修复 Sticky 世界卡在每次提交后自我续期、Cooldown 无法开始所导致的重复叙事风险；关联卡同步遵循作用域、冷却、互斥组和预算。
- 收口世界卡任务协议，并让 Horizon 使用自身任务上下文；v8 世界包明确只以 worldCards 为静态召回来源。
- 抉择上下文可精确继承当前场景刚提交的地点与本领；三个世界的风格卡改为真实短叙事样例。
- 清理动态导演未消费参数，新增生命周期、任务矩阵、版本策略与资产召回回归及后端诊断信息。游戏状态机和前端接口不变。

## 2026-09-16 11:48 +08:00 — 情景召回世界卡

- 世界卡协议升级至 v8，增加正文关键词扫描、辅助选择逻辑、注入位置、关联召回、粘滞与冷却。
- 上下文改为按当前情景召回世界资料、表达样例和动态人物／地点／本领／事实；工具引用目录不再随整局状态全量增长。
- 古代包退出旧组件事件簿运行链，现代和奇幻补齐可触发的生活、制度、地理、能力与冲突素材；三套世界包保留原五拍、路线和结局配置。
- 增加世界卡设计基线、v8 社区示例、编写说明、关键词召回测试与后端调试追踪。

## 2026-09-15 13:21 +08:00 — 叙事 Agent 阶段 6—8

- 运行态升级至 v9，增加异步分作用域 Memory Curator；替代旧 conversation archive 摘要调度，并以 revision、Episode 覆盖范围和匿名存档锁保证异步提交归属。
- 增加幕间短程 Horizon。它只向单回合 Planner 提供跨回合叙事意图，世界包所有路线继续开放，普通年份不增加该请求。
- 增加统一动态 Narrative Agent Runtime、正文整理工具、Context Provider 顺序和内部 Attempt 引用记录。正文整理不允许改变任何结构化游戏结果。
- 新增长期记忆、精确追溯、Horizon 生命周期和 Provider 顺序测试；后端 64 项回归、前后端构建与 shared `--noEmit` 检查通过。shared emit 因本地进程占用输出文件返回 EPERM；未终止用户进程、启动后端或调用真实模型。

## 2026-09-15 12:32 +08:00 — 叙事 Agent 阶段 3—5

- 增加受控回合 Planner；动态场景先选回合意图、路线、阵营与关注引用，再按计划二次召回并调用唯一 Renderer 工具。纯背景不额外增加规划请求。
- 上下文任务拆分为 planning/rendering/closure 等真实职责，并为 manifest 增加 call/source/world/focus 关联；历史按完整语义回合投影，不做字符级裁剪。
- 身世、动态场景和结局接入失败不提交的事务边界；结局渲染移除固定字符裁剪与本地文案回退。
- 运行态升级到 v8，新增引用型 Episode 与 Act Canon，用于按活动场景、显式关注、当前幕和前幕结果召回连续性材料，不复制玩家正文。
- 新增规划工具收窄、事务失败不污染状态、Episode/Canon 引用召回回归。后端 61 项测试、前后端构建通过。

## 2026-09-15 11:06 +08:00 — 叙事上下文收口为统一编排器

- 增加六层内部上下文协议、任务预算、来源去重、统一 formatter 和结构化 Prompt Trace。
- 将身世、普通人生段、主线场景和抉择后果的工具请求接入同一 Context Orchestrator；conversation 摘要、待摘要 archive 与近期回合不再由请求层另行拼接。
- 为 Lore、事实、人物、地点、本领和局内记忆补充稳定召回来源，合并事实目录与详情的重复投影。
- 新增编排来源唯一性和预算裁剪回归；没有改变游戏状态机、模型工具能力、存档及前端接口，也没有增加模型调用次数。
- 后端 59 项回归、后端编译与前端生产构建通过；未启动服务、调用真实模型或修改本地存档。

## 2026-09-15 01:50 +08:00 — 修复抉择记忆归属与混合回合召回

- 统一抉择事实、局内记忆与会话记录的来源 ID，抉择形成的事实进展和结果可进入待摘要变化。
- 普通人生段与主线场景共存时，公共上下文不再预先绑定当前幕事实或随机路线 Lore；场景专属信息继续由场景工具提供。
- 生活侧保留可更新的动态事实引用，世界幕裁决权和现有游戏状态机保持不变。
- 后端 57 项回归通过，前后端编译通过；未启动服务、调用真实模型或修改存档。


## 2026-09-15 01:34 +08:00 — memory 与 prompt 整合

- 统一现有请求入口的层次，历史摘要作为数据投影，去除公共上下文重复的正文任务要求。
- 事实变化同步表达状态与内容；人物档案支持同步更新处境与离场状态，并复用公开回合展示。
- 分离直接查询与召回结果，减轻人物、本领、地点和历史互相放大；已完成事项保留结果，已被摘要覆盖的经历减少间接重复注入。
- 待摘要窗口优先使用分类变化记事，原始正文仍保留并异步压缩；增加摘要覆盖来源，沿用分支归属校验。
- 场景准入结果明确传入任务，不增加推进门槛、强制事件或调用次数。
- 57 项回归与前后端编译通过；未启动后端、未调用真实模型、未修改存档。实际生成表现需后续游玩确认。


## 2026-09-10 00:26 +08:00 - 文档与 README 对齐（增量）

- README 同步古代／奇幻／现代三世界、生活成长与主线任务分层、生存危机、三档结局、足迹本领、匿名分支存档及模型用量。
- 配置和世界包指南明确动态世界选择、首次初始化与既有内容仓储的区别，提供已有 JSON／TXT 模板入口。
- 架构、技术、使用流程与开发日志增加当前链路说明；早期正文按历史记录保留。
- 后续方向为数据库存储和前端 UX 交互，当前仍使用文件仓储。
- 本轮是文档更新，核验代码与本地链接；未执行编译或游戏测试。前轮编译与 53 项回归结果仍归属于前轮记录。

## 2026-09-09 23:47 +08:00 - 三世界阶段中心、普通年份与历史召回（增量）

- 古代、奇幻、现代世界包分别明确案件入局→阵营立身→战乱担当、成长入道→宗门历练→破境问道、学习成长→选择迷茫→关系成就的阶段中心。幕说明强调事情的实际结果和人物处境，沿用 v6 格式及既有路线、门槛和结算配置。
- 动态叙事的场景工具承载总纲、当前幕及节拍任务；背景工具承载生活与成长。混合请求仍由模型在同一次调用中选择，普通年份不再统一承担场景推进指令。
- 事实详情按任务和相关性召回；完整开放事实引用目录保留。已收束事实使用结果摘要，关联历史记忆按时间降低间接召回权重；直接事实、本领使用和结局任务仍可召回相关历史。
- 验证：后端编译通过，53 项回归通过，覆盖三世界实际请求投影、完整路线与事实引用、已结束事件降权及存档不被读取操作修改。未启动服务或调用真实模型，实际叙事自然度仍需游玩验证。

## 2026-09-09 12:30 +08:00 - 古代三幕叙事与幕间记忆（增量）

- 古代世界包沿用 v6 格式，三幕分别突出身边纷争的实际处理、新位置中的独立挑战、现实危机中的行动；阵营与 Lore 补充资源、诉求和组织能力。
- 动态请求保留全量路线目录，按当前节拍读取路线视角、升级、危机或收束指引；任务上下文消费世界总纲，Lore 按当前任务和实际节拍召回。
- `actHandoff` 记录已形成的结果、后果与新处境；真正未完成的事项继续通过 `factUpdates` 管理。交接事实关联同一回合记忆及人物。
- 旧存档的幕间后果按已发生历史处理，保留 ID；旧幕间承诺保持开放并可通过事实工具收束，幕完成事实仍由引擎控制。
- 新幕开场集中提供前幕处境，后续按相关性召回。年龄、属性、五拍推进、路线候选与结局计数沿用现有链路。
- 验证：后端编译通过，50 项回归通过，覆盖三幕完成、旧事实更新、快照隔离、三个世界包任务输入及原有公开投影。未启动后端服务、未调用真实模型；实际叙事表现仍需游玩采样。

## 2026-08-25 - 生存危机、天赋叙事与命运人物（增量）
- 古代世界包新增并启用按年龄阶段配置的体魄生存风险：连续低体魄经过宽限期与概率审核后才进入危机，避免单次属性波动直接终止人生。
- 新增自愈、请求帮助、听天由命三种生存选择，分别依赖智力、魅力、气运档位；成功恢复体魄，失败才进入带确定性死因的死亡结算。
- 家境档位在每个实际跨过的年份按世界包权重提供体魄支持，作为长期生存曲线的一部分。
- 天赋卡统一为数值修正与紧凑叙事画像：玩家选定的 1 至 3 张卡会影响开局属性、身世、场景和结局上下文，前端在身世折叠页与侧栏展示。
- 命运人物改用稳定 `characterRef`。常驻人物复用既有身份；遗留 `new` 引用会按姓名和阵营收敛，修复同一人物在前端重复投影的问题。

## 2026-08-23 - 叙事节奏与成长选择（增量）
- 世界包升级支持四档属性门槛、三种幕间成长方向和属性档位配置；引擎继续与具体世界和路线数量解耦。
- `compose_world_scene` 合并场景推进与普通人生段选择，背景段不会再触发第二次模型调用，也不再依赖普通年份内部标签。
- 重要拍点的抉择密度改为：压力/高潮必有，setup/escalation 可按场景后果产生；玩家选择后才推进当前拍。
- 匿名存档保存成长方向；前端显示属性档位和成长方向选择卡，不显示内部叙事标签或工具协议。

## 2026-08-22 04:29 +08:00 - 动态世界幕导演（增量）
- 新增世界包 v4 契约：世界幕事实、阵营目录、全局五拍运行态、局内人物和本地记忆条目。
- 活跃叙事链改为模型 `compose_world_scene` 工具调用；模型选择世界包路线/阵营并渲染场景，引擎保留年龄、属性、死亡、事实、幕推进和结局的唯一裁决权。
- 抉择的高潮后果可提交受限事实收束方式，`payoff` 后才进入下一世界幕；三幕完成后才允许结局申请。
- 前端将命运栏改为展示局内常驻“命运人物”，不复用道具数据。

## 2026-08-22 01:34 +08:00 - 叙事属性平衡与三档结局（增量）
- 将导演路径的模型属性结果统一为“模型提议语义后果、引擎按策略审批并换算数值”的单次工具调用链；没有恢复随机属性 roll。
- 普通背景年份固定在轻/中度正向成长；抉择改为可验证的三档：稳健仅轻度正收益，适中可有轻代价但必须保留收益，冒险可出现重度突破或重度挫折。
- 名望由属性基础、已完成世界幕与真实抉择结果共同构成；它以受限修正量参与叙事结局评分，不替代主线完成判定。
- 结局蓝图从好/坏扩展为好/普通/坏三档。古代六条路线均补齐普通结局蓝图；内容加载会校验每条配置路线同时具备三档蓝图。
- 后端回归测试 23/23 通过，后端 TypeScript 构建通过。共享包构建的 `dist` 被已有本地进程占用，未强制终止该进程。

## 2026-08-21 01:29 +08:00 - 路线局部节拍与世界幕（增量）
- 新增按世界包 `routeArcs` 派生的 `routeProgress`，路线数不再由引擎或古代世界硬编码。
- 导演工具继续由模型选择路线 ID；引擎保留既有事件 `narrativeBeat`，按所选路线的局部节拍确定具体素材。
- 移除 `activeScene` 对其它路线候选的全局阻断。它现在只负责当前路线的场景展示与年龄计时投影。
- payoff 后只重置达成该 payoff 的路线；其它路线的局部进度和已发生事实保留。当前世界幕推进到下一个 `mainlineAct`。
- `thread:*` 不再在叙事模式中造成跨幕、跨路线候选阻断，仍作为事实账本和素材关联记录；旧非叙事事实校验不变。
- 路线选材接口改为直接接收 `routeId`，新世界包不必依赖旧 `StoryDirectionDefinition` 才能进入导演链。
- 后端回归测试 21/21 通过，后端 TypeScript 编译通过；共享包构建输出被本地占用，未强制终止占用进程。

## v1.0.2 - 2026-06-02
- 新增小说/长篇素材蒸馏操作文档：
  - `docs/NOVEL_DISTILLATION_GUIDE.md`
- 文档覆盖：
  - 如何分章摘要小说素材
  - 如何用 AI 蒸馏世界观圣经
  - 如何生成 `worlds/worldlines/factions/faction-events/talent-cards/promptPack`
  - 如何同步到 `storage/custom-content.json`
  - 如何做本地跑局验证与复读排查
- `README.md` 文档导航新增该入口。

## v1.0.1 - 2026-06-01
- 修复“第二个抉择后卡死”问题（前端推进状态机）：
  - `pendingAdvanceCountRef` 在 `runStepGeneration` 结束时统一释放，避免等待计数悬挂
  - 时间线缓冲改为基于 `timelineBufferRef` 的同步去重入队，减少异步 `setState` 竞态
- 抉择链路稳定性增强：
  - 抉择请求年龄透传与前端乐观态切换保持一致，降低 `waiting_decision` 残留导致的阻塞概率
- 文档同步更新：
  - `README.md`
  - `docs/USAGE_FLOW.md`
  - `docs/TECHNICAL.md`
  - `docs/ARCHITECTURE.md`

## v1.0.0 - 2026-05-30
- 文档全面对齐当前版本实现：
  - 重写 `docs/USAGE_FLOW.md`
  - 重写 `docs/CONFIG_GUIDE.md`
  - 重写 `docs/ARCHITECTURE.md`
  - 重写 `docs/TECHNICAL.md`
  - 重写 `docs/DEPLOYMENT.md`
- 关键对齐点：
  - 根目录 `.env` 读取路径（后端以 `process.cwd()` 为准）
  - 后端固定读取根目录 `data/*` 与 `skills/ai-gm/prompt-pack.json`
  - 抉择触发逻辑：阶段概率 + 保底 + 最低年龄门槛
  - 流式事件顺序：`started -> timeline -> meta -> milestone -> done`
  - `started` 阶段不下发 milestone（避免前端先挂 fallback 文案）
  - 前端“抉择历史”改为在收到 milestone 年份 AI 文本后再挂载，并展示掷点胶囊

## v0.8.1 - 2026-05-28
- 新增 GitHub 发布准备文档：
  - `docs/GITHUB_RELEASE_CHECKLIST.md`
  - `docs/VSCODE_SOURCE_CONTROL_GUIDE.md`
- `README.md` 文档导航新增上传检查与 VS Code 提交流程入口。
- 发布前安全审计结论补充：
  - 重点检查 `apps/backend/.env` 不入库
  - 路径/密钥扫描需排除子目录依赖缓存（如 `apps/frontend/node_modules/.vite`）
  - 本地 `.env` 变量名与 `.env.example` 可能不一致，需上传前人工对齐

## v0.8.0 - 2026-05-28
- 文档体系重整：
  - 新增 `docs/TECHNICAL.md`（技术文档）
  - 新增 `docs/USAGE_FLOW.md`（使用流程）
  - 新增 `docs/DEV_LOG.md`（开发日志）
  - 重写 `docs/ARCHITECTURE.md`（分层架构 + 运行时序）
- `README.md` 增加“文档导航”并统一指向各文档入口。
- 文档内容按当前实现对齐：
  - start/step API 编排
  - 年份推进与里程碑决策逻辑
  - 本地/云端模式边界
  - 配置与持久化路径

## v0.7.2 - 2026-05-28
- 文本长度控制改为纯提示词约束：\n  - 年份总结/事件背景：80~150字\n  - 选项与对话：<=20字
- 移除后端对年份与背景文本的硬截断阈值处理（不再依赖字符裁切）。

## v0.7.1 - 2026-05-28
- 补全 Phase A 提示词输入字段：\n  - 当年属性变化(delta)\n  - 当前属性绝对值（含体魄）\n  - 当前名望值与档位\n  - 是否经过空过年份\n  - 当前年龄阶段\n  - 当前风险等级（A/B/C或普通年）
- 年度与节点背景长度规则强化：180~300字；选项<=30字。
- `ProviderConfigForm` 中 `Max Tokens` 改为只读（由后端固定控制）。

## v0.7.0 - 2026-05-28
- 执行 Phase A 规则重构：
  - 新增开局维度 `体魄`
  - 开局随机总点改为 `20~30`
  - 引入 `名望(fame)` 与 `结局状态(outcome)`
- 结局判定重构：
  - 仅 `死亡` 或 `飞升`
  - 支持 `deathCause` 记录
- 年份状态机更新：
  - 年度随机增减五维属性
  - 死亡风险与体魄/名望联动
  - 飞升阈值为任一关键属性达到 30
- 三选项风险收益重映射：
  - A低风险低收益，B中风险中收益，C高风险高收益
  - C可触发死亡结束
- 前端面板新增 `体魄` 显示，运行态新增 `名望` 与 `结局状态` 展示。
- 文档同步更新：架构、配置、变更记录。

## v0.6.0 - 2026-05-27
- 新增部署双链路：
  - `start-local.bat`（本地部署链路）
  - `start-cloud.bat`（云端体验链路）
- `start.bat` 改为本地链路入口代理。
- 启动模式由工程入口决定，不再在局内切换。
- 后端接入 `DEPLOY_MODE`，并在 `/api/meta/bootstrap` 返回 `deployMode`。
- Setting 中移除运行模式切换，仅显示当前部署链路。
- 新增部署手册：`docs/DEPLOYMENT.md`。

## v0.5.0 - 2026-05-27
- 新增模块化设定源：
  - `data/settings/worldlines/*.timeline.json`
  - `data/settings/factions/factions.json`
  - `data/events/faction-events.json`
  - `data/talents/talent-cards.json`（20张天赋卡+prompt钩子）
- 后端 AI 上下文接入：世界线摘要、阵营摘要、阵营事件池摘要、已选天赋卡叙事钩子摘要。
- 提示词工程细化并固化字段：
  - `yearNormalRule`
  - `yearMinorRule`
  - `milestoneRule`
  - `storyConstraint`
- `data/cards.json` 同步为 20 张卡的运行卡池（从 talent-cards 派生）。
- 文档更新并标注设定集具体路径。

## 2026-08-15 01:22 +08:00 - 叙事世界包与 P4 结局链（增量记录）
- 古代世界接入可配置主线骨架、路线属性门槛、事实账本、背景段落节拍、活跃场景与双向结局蓝图。
- 导演工具从“模型选择事件方向”收紧为“模型提出叙事意图、引擎审批具体候选”；模型不能修改事件数值、掉落、路线事实或结局极性。
- 主线完成后只允许 `request_story_closure`；引擎锁定结局蓝图后才渲染收束和结算。年龄不再是叙事世界包的强制结局条件。
- 结局工具协议异常时保留结局引导并阻止无关年份推进；兼容存档会在背景段落前刷新完成状态。
- 文档以追加方式同步：`ARCHITECTURE.md`、`TECHNICAL.md`、`USAGE_FLOW.md`、`DEV_LOG.md`。
